services:
  - type: web
    name: autodub-pro
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py --web --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9